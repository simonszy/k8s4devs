apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: pluscode-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          serviceName: pluscode-service
          servicePort: 80